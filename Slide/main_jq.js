var aBtn = document.querySelectorAll('.buttonBox span');
var len = aBtn.length;
for(let i=0;i<len;i++){
    aBtn[i].on('click',function(e){
        let index = $(e.targetElement)
        $('.content img').eq()
    })
}